#include <stdio.h>

/**
 * main - prints the script with the printf function
 *
 * Description - uses the main function
 * This programs pirnts the statement "with proper grammar,
 * but the outcome is a piece of art"
 * Return: 0
 */
int main(void)

{
	printf("with proper grammar, but the outcome is a piece of art\n");
	return (0);
}
